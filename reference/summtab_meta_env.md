# Metadata environment for summary table calculation

This environment is used to temporarily store denominator values for
percentage calculation across categories.

## Usage

``` r
summtab_meta_env
```

## Format

An object of class `environment` of length 0.
