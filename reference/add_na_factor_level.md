# Replace NA values in a factor with character value and add associated level

Replace NA values in a factor with character value and add associated
level

## Usage

``` r
add_na_factor_level(f, level = "<NA>")
```

## Arguments

- f:

  Factor

- level:

  New level to associate with original NA values

## Value

A factor with NA replaced with character value/level
