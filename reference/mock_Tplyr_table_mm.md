# Mock app integrated in the module manager

`mock_Tplyr_table_mm` launches a mock app for the Tplyr_table shiny
module by means of the module manager (dv.manager).

## Usage

``` r
mock_Tplyr_table_mm()
```
