# Mock summary table app integrated in the `{dv.manager}` module manager framework

Mock summary table app integrated in the `{dv.manager}` module manager
framework

## Usage

``` r
mock_app_summary_table_mm()
```
