# Mock app tabbed UI layout

`mock_Tplyr_table_tabs` launches a mock app for the Tplyr_table shiny
module with tab UI layout (default is drop-down selector).

## Usage

``` r
mock_Tplyr_table_tabs()
```
