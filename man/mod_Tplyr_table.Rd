% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_Tplyr_table.R
\name{mod_Tplyr_table}
\alias{mod_Tplyr_table}
\title{Typlr table module for DaVinci's module manager}
\usage{
mod_Tplyr_table(
  module_id,
  output_list,
  subjid_var = "USUBJID",
  default_vars = NULL,
  pagination = NULL,
  intended_use_label =
    "Use only for internal review and monitoring during the conduct of clinical trials.",
  receiver_id = NULL,
  review = NULL
)
}
\arguments{
\item{module_id}{\verb{[character(1)]}

A character string that serves as unique identifier for the module.}

\item{output_list}{\verb{[list(list())]}  A named list defining the outputs to be generated. Each element of the list
corresponds to a table or listing and must be a named list with one of the following structures:

For summary tables:
\describe{
\item{tplyr_tab_fun}{A function that takes one or more datasets as input and returns a \code{tplyr_table} object.}
\item{build_fun}{A function that takes the \code{tplyr_table} object and returns a built table (typically using \code{Tplyr::build()}).
The metadata argument of \code{Tplyr::build()} needs to be set to \code{TURE}, so that the corresponding listing can be shown.}
}

For listings:
\describe{
\item{dataset_names}{A character vector of dataset names required to generate the listing.}
}

The names of the top-level list elements are used as identifiers for the outputs.}

\item{subjid_var}{\verb{[character(1) | NULL]}

Column corresponding to subject ID. Default value is 'USUBJID'}

\item{default_vars}{\verb{[list(characters(1+)) | NULL]}

A list of character vectors which contain the variable names to be displayed as default per
dataset. Named according to the datasets in the \code{tplyr_tab_fun}. If \code{NULL}, the first six variables are displayed for each
dataset.}

\item{pagination}{\verb{[logical(1) | NULL]} Either a boolean indicating if pagination should be activated, or
NULL for which pagination will be activated for large datasets (nrows > 1000) automatically.

@param on_sbj_click \verb{[function()]}

Function to invoke when a subject ID is clicked in a listing}

\item{intended_use_label}{\verb{[character(1) | NULL]} Either a string indicating the intended use for export, or
NULL. The provided label will be displayed prior to the download and will also be included in the exported file.}

\item{receiver_id}{\verb{[character(1) | NULL]}

Character string defining the ID of the module to which to send a subject ID. The
module must exist in the module list. The default is NULL which disables communication.}
}
\description{
Typlr table module for DaVinci's module manager
}
