% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_hierarchical_count_table.R
\name{create_adtte}
\alias{create_adtte}
\title{Create analysis data in the form of an ADaM ADTTE dataset}
\usage{
create_adtte(
  event_df,
  pop_df,
  hierarchy = character(),
  group_var,
  subjid_var,
  origin_date_var,
  censor_date_var,
  event_date_var
)
}
\arguments{
\item{event_df}{\code{data.frame}
A data frame containing the event data. It should have columns corresponding to subjects,
hierarchy levels, and group variables.}

\item{pop_df}{\code{data.frame}
A data frame containing the population data. It must have columns corresponding to subjects and group variables.}

\item{hierarchy}{\code{character(1|2)}
A character vector of column names from \code{event_df} to use as the hierarchy. Can be one or two levels.}

\item{group_var}{\code{character(1)}
A string representing the column name in \code{pop_df} used for grouping the population data.}

\item{subjid_var}{\code{character(1)}
A string representing the subject identifier column name. This column must be present in both \code{event_df} and
\code{pop_df}.}

\item{origin_date_var}{\code{character(1)}
A string representing the column name in \code{pop_df} holding the origin date.}

\item{censor_date_var}{\code{character(1)}
A string representing the column name in \code{pop_df} holding the censor date.}

\item{event_date_var}{\code{character(1)}
A string representing the column name in \code{event_df} holding the event date.}
}
\value{
A data frame in the form of an ADaM ADTTE, with the addition of hierarchy columns and hierarchy level.
}
\description{
Create analysis data in the form of an ADaM ADTTE dataset
}
\keyword{internal}
