% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_hierarchical_count_table.R
\name{compute_events_table}
\alias{compute_events_table}
\title{Computes an event table with subject counts and percentages}
\usage{
compute_events_table(
  event_df,
  pop_df,
  hierarchy = NULL,
  group_var = NULL,
  subjid_var = NULL,
  event_group_var = NULL,
  origin_date_var = NULL,
  censor_date_var = NULL,
  event_date_var = NULL,
  total = TRUE,
  total_group_val = "Total",
  compute_risk = FALSE
)
}
\arguments{
\item{event_df}{\code{data.frame}
A data frame containing the event data. It should have columns corresponding to subjects,
hierarchy levels, and group variables.}

\item{pop_df}{\code{data.frame}
A data frame containing the population data. It must have columns corresponding to subjects and group variables.}

\item{hierarchy}{\code{character(1|2)}
A character vector of column names from \code{event_df} to use as the hierarchy. Can be one or two levels.}

\item{group_var}{\code{character(1)}
A string representing the column name in \code{pop_df} used for grouping the population data.}

\item{subjid_var}{\code{character(1)}
A string representing the subject identifier column name. This column must be present in both \code{event_df} and
\code{pop_df}.}

\item{event_group_var}{\code{character(1)}
A string representing the column name in \code{event_df} used for grouping the event data.}

\item{origin_date_var}{\code{character(1)}
A string representing the column name in \code{pop_df} holding the origin date (optional).}

\item{censor_date_var}{\code{character(1)}
A string representing the column name in \code{pop_df} holding the censor date (optional).}

\item{event_date_var}{\code{character(1)}
A string representing the column name in \code{event_df} holding the event date.}

\item{total}{\code{logical(1)}
A logical indicating whether to add a total group column.}

\item{total_group_val}{\code{character(0|1)}
A string representing the value to assign to the \code{group_var} column for totals.}

\item{compute_risk}{\code{logical(1)}
A logical indicating whether to calculate time at risk and incidence rate.}
}
\value{
A list containing:
\itemize{
\item \code{df}: A data frame with the processed event data, including counts and percentages.
\item \code{meta}: A list containing metadata related to the hierarchy, group variable, and subject counts.
}
}
\description{
Computes an event table with subject counts and percentages
}
\keyword{internal}
